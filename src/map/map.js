class Map {
    constructor(rows, cols) {
        this._rows = rows;
        this._cols = cols;
        this.init(layout);
    }

    get rows() {
        return this._rows;
    }

    get cols() {
        return this._cols;
    }

    init() {
        // set up map 
    }
}